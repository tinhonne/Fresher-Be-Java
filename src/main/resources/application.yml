spring:
  profiles:
    default: dev

app:
  jwt:
    issuer: ${JWT_ISSUER:test.vn}
    expiration: ${JWT_EXPIRATION:1h}
  security:
    password-strength: ${PASSWORD_STRENGTH:10}
    cors:
      allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
      max-age: ${CORS_MAX_AGE:1h}
